const GameReducer = (state, action) => {};

export default function reducer(history, action) {
	if (action.type === "move") {
	}

	if (action.type === "undo") {
	}

	if (action.type === "redo") {
	}

	if (action.type === "reset") {
	}
}
